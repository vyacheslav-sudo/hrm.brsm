
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    Если Параметры.Отбор.Свойство("УчетнаяСистема") Тогда
        Элементы.УчетнаяСистема.Видимость = Ложь;
        АвтоЗаголовок = Ложь;
        Заголовок = НСтр("ru='Состояния интеграции объектов';uk='Стан інтеграції об''єктів'"); 
    ИначеЕсли Параметры.Отбор.Свойство("ИдентификаторОбъекта") Тогда
        Элементы.ИдентификаторОбъекта.Видимость = Ложь;    
        АвтоЗаголовок = Ложь;
        Заголовок = НСтр("ru='Состояния интеграции по учетным системам';uk='Стани інтеграції за обліковими системами'"); 
    КонецЕсли;

КонецПроцедуры

#КонецОбласти 
