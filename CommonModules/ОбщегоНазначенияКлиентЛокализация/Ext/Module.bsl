
#Область ПрограммныйИнтерфейс

// Выполняется перед интерактивным началом работы пользователя с областью данных или в локальном режиме.
// Соответствует обработчику ПередНачаломРаботыСистемы.
//
// см. ОбщегоНазначенияКлиентПереопределяемый.ПередНачаломРаботыСистемы()
//
Процедура ПередНачаломРаботыСистемы(Параметры) Экспорт
	
	//++ Локализация
	ПараметрыРаботыКлиента = СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиентаПриЗапуске();
	
	Если ПараметрыРаботыКлиента.Свойство("ВозможенЗапускБазовойВерсии")
	 И НЕ ПараметрыРаботыКлиента.ВозможенЗапускБазовойВерсии Тогда
		Параметры.Отказ = Истина;
		ВызватьИсключение НСтр("ru='Базовая версия может использоваться только в файловом однопользовательском режиме.';uk='Базова версія може використовуватися тільки у файловому однокористувацькому режимі.'");
	КонецЕсли;
	
	//++ НЕ УТ
	
	// ЗарплатаКадрыРасширенный
	ЗарплатаКадрыРасширенныйКлиент.ПередНачаломРаботыСистемы(Параметры);
	// Конец ЗарплатаКадрыРасширенный
	
	//-- НЕ УТ
	//-- Локализация
	
КонецПроцедуры

// Выполняется при интерактивном начале работы пользователя с областью данных или в локальном режиме.
// Соответствует обработчику ПриНачалеРаботыСистемы.
//
// см. ОбщегоНазначенияКлиентПереопределяемый.ПриНачалеРаботыСистемы() 
//
Процедура ПриНачалеРаботыСистемы(Параметры) Экспорт
	
	//++ Локализация
	ПараметрыРаботыКлиента = СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиентаПриЗапуске();
	
	Если ПараметрыРаботыКлиента.ДоступноИспользованиеРазделенныхДанных Тогда
		
		#Если НЕ ВебКлиент Тогда
		Если НЕ ПараметрыРаботыКлиента.РазделениеВключено
			И ПараметрыРаботыКлиента.ЭтоГлавныйУзел Тогда
			// ИнтернетПоддержкаПользователей
			ИнтернетПоддержкаПользователейКлиент.ПриНачалеРаботыСистемы(Параметры);
			// Конец ИнтернетПоддержкаПользователей
		КонецЕсли;
		#КонецЕсли
		
		
	КонецЕсли;
	//-- Локализация
	
КонецПроцедуры

// Выполняется при интерактивном начале работы пользователя с областью данных или в локальном режиме.
// 
// см. ОбщегоНазначенияКлиентПереопределяемый.ПослеНачалаРаботыСистемы()
//
Процедура ПослеНачалаРаботыСистемы() Экспорт
	
	//++ Локализация
	//++ НЕ УТ
	ПараметрыРаботыКлиента = СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиентаПриЗапуске();
	
	Если ПараметрыРаботыКлиента.ДоступноИспользованиеРазделенныхДанных Тогда
		
		Если ПараметрыРаботыКлиента.ПоказатьПутеводительПоДемоБазе Тогда
		
			ОткрытьПутеводительПоДемоБазе(ПараметрыРаботыКлиента.ИмяОбработкиПутеводительПоДемоБазе);
		
		КонецЕсли;
		
	КонецЕсли;
	//-- НЕ УТ
	
	
	НСИСКлиент.ПослеНачалаРаботыСистемы();
	//-- Локализация
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

////////////////////////////////////////////////////////////////////////////////
// Открытие путеводителя по демо-базе

Процедура ОткрытьПутеводительПоДемоБазе(ИмяОбработки) Экспорт
	
	#Если НЕ ТолстыйКлиентОбычноеПриложение Тогда
	Если ЗначениеЗаполнено(ИмяОбработки) Тогда
		ОткрытьФорму("ВнешняяОбработка."+ ИмяОбработки +".Форма");
	КонецЕсли;
	#КонецЕсли
	
КонецПроцедуры

#КонецОбласти
