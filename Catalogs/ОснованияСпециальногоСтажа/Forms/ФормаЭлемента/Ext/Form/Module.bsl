
&НаКлиенте
Процедура ОграничениеПоПолуПриИзменении(Элемент)
	
	ДоступностьЭлементаПолФизическогоЛица();
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ДоступностьЭлементаПолФизическогоЛица();
	
КонецПроцедуры


Процедура ДоступностьЭлементаПолФизическогоЛица()
	
	Элементы.ПолФизическихЛиц.Доступность = Объект.ОграничениеПоПолу;
	Если НЕ Объект.ОграничениеПоПолу Тогда
		Объект.ПолФизическихЛиц = ПредопределенноеЗначение("Перечисление.ПолФизическогоЛица.ПустаяСсылка");
	КонецЕсли;		
	
КонецПроцедуры	

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Объект.ОграничениеПоПолу И Объект.ПолФизическихЛиц = ПредопределенноеЗначение("Перечисление.ПолФизическогоЛица.ПустаяСсылка") Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не выбран пол физических лиц, элемент не записан");
		Отказ = Истина;
	КонецЕсли;	
		
КонецПроцедуры
