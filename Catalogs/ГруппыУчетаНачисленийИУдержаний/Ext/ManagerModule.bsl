#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает текст запроса для получения параметров группы учета.
//
Функция ТекстЗапросаДанныхГруппУчета(ИмяВременнойТаблицы = "") Экспорт
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ГруппыУчетаНачисленийИУдержаний.Ссылка КАК ГруппаУчета,
	|	ГруппыУчетаНачисленийИУдержаний.СчетУчета КАК СчетУчета,
	|	ГруппыУчетаНачисленийИУдержаний.СубконтоУчета КАК СубконтоУчета
	|%ПоместитьВоВременнуюТаблицу%
	|ИЗ
	|	Справочник.ГруппыУчетаНачисленийИУдержаний КАК ГруппыУчетаНачисленийИУдержаний
	|";
	Если ЗначениеЗаполнено(ИмяВременнойТаблицы) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "%ПоместитьВоВременнуюТаблицу%", "ПОМЕСТИТЬ " + ИмяВременнойТаблицы);
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "%ПоместитьВоВременнуюТаблицу%", "");
	КонецЕсли;
	
	Возврат ТекстЗапроса;
	
КонецФункции

#КонецОбласти

#КонецЕсли