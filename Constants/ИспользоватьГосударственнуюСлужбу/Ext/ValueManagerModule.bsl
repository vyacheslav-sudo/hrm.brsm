
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ПередЗаписью(Отказ)
	
	ПодсистемаСуществует = ОбщегоНазначения.ПодсистемаСуществует("ЗарплатаКадрыПриложения.ГосударственнаяСлужба");
	Если Не ПодсистемаСуществует Тогда
		Если Значение Тогда
			ВызватьИсключение НСтр("ru='Нельзя установить значение ИспользоватьГосударственнуюСлужбу';uk='Не можна встановити значення ИспользоватьГосударственнуюСлужбу'");
		КонецЕсли;
	КонецЕсли;
	
	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	Если ПодсистемаСуществует Тогда
		ИспользоватьМуниципальнуюСлужбу = Константы.ИспользоватьМуниципальнуюСлужбу.Получить();
		Если Значение И ИспользоватьМуниципальнуюСлужбу Тогда
			ВызватьИсключение НСтр("ru='В программе уже ведется расчет денежного содержания Муниципальных служащих, не допускается использовать одновременно расчет денежного содержания Государственных и Муниципальных служащих';uk='У програмі вже ведеться розрахунок грошового утримання Муніципальних службовців, не допускається використовувати одночасно розрахунок грошового утримання Державних і Муніципальних службовців'");	
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ЭтотОбъект.Значение И НЕ Константы.ИспользоватьУчетКлассныхЧинов.Получить() Тогда
		Константы.ИспользоватьУчетКлассныхЧинов.Установить(Истина);
	ИначеЕсли Не ЭтотОбъект.Значение И Константы.ИспользоватьУчетКлассныхЧинов.Получить() Тогда
		Константы.ИспользоватьУчетКлассныхЧинов.Установить(Ложь);	
	КонецЕсли;
	
КонецПроцедуры

#КонецЕсли
