#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ЗагрузитьКурсы(Объект, АдресРезультата) Экспорт
	
	СписокВалют = Новый Массив();
	Для Каждого Валюта из Объект.Валюты Цикл
		Если Валюта.Пометка Тогда
			СписокВалют.Добавить(Число(Валюта.Код));	
		КонецЕсли;	
		
	КонецЦикла;	
		
	НСИССервер.ВыполнитьОбновлениеКурсовВалют(Объект.ДатаС,Объект.ДатаПо,СписокВалют,Истина,ЗначениеЗаполнено(АдресРезультата));
	
КонецПроцедуры

#КонецЕсли
