
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ПустаяСтрока(Параметры.АдресХранилищаПодписок) Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru='В форму не передан обязательный параметр ""АдресХранилищаПодписок"".';uk='До форми не переданий обов''язковий параметр ""АдресХранилищаПодписок"".'"),,,, Отказ);	
		Возврат;
	КонецЕсли;
	
	Подписки = ПолучитьИзВременногоХранилища(Параметры.АдресХранилищаПодписок);
	ДанныеПодписок.Загрузить(Подписки);
	
КонецПроцедуры

#КонецОбласти

